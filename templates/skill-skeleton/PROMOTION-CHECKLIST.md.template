# Promotion Record — <name>

**Skill**: <name>
**Promoted**: YYYY-MM-DD  (`staging/<name>/` → `.claude/skills/<name>/`)

> Mechanical gates — harness 0 FAIL, `tests.md` ≥3 scenarios with a fresh
> `Last verified:` date, naming, sidecar linking — are machine-enforced by
> `eval/check-skill.sh` and CI, so they are not re-attested here. This file
> records only what a human (or judging agent) can attest.

## Real-work evidence

<What the 2–3 manual runs were: inputs, dates, and where the captures live.
This is the "built from doing the task, not from imagination" attestation.>

## Security review

<Which harness WARNs were reviewed and why they're acceptable. For skills that
touch credentials, browsers, or external systems: note the constraints that
make them safe (and that you actually read the scripts/references, not just
SKILL.md).>

## Value-add verdict

- [ ] **Value-add verdict**: PASS | CONCERN | FAIL  (date, lift /50, # seeds) — or **N/A (non-generative)**

<Generative/judgment skills only — run `eval/value-add-test.md`. A CONCERN or
FAIL must be acknowledged here with a fix or a written justification, never
silently ignored. Check 15 reads this line; check the box only when the
verdict is real.>

## Strip list

<Exact staging-only files removed from the promoted copy (`goal.md`, `goals/`,
raw evidence logs…), per CLAUDE.md "Skill location rules" — or "none".>

## Live test

<Date + the real request(s) exercised in a fresh session after promotion,
confirming the skill auto-loads and fires.>
