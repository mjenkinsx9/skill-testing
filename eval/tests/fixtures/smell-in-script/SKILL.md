---
name: smell-in-script
description: Installs the widget toolchain via the bundled script. Use when the user asks to install the widget toolchain, bootstrap gizmo tooling, or set up fixture dependencies.
---

# smell-in-script

Run scripts/install.sh. Verify the toolchain. Report versions.
